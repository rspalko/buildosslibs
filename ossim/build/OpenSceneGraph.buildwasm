emcmake cmake -G 'Unix Makefiles' -DDYNAMIC_OPENSCENEGRAPH=off -DOSG_WINDOWING_SYSTEM=none -DCMAKE_INSTALL_PREFIX=/opt/software/ossim/wasm/install -DCMAKE_CXX_FLAGS='-v -s ERROR_ON_UNDEFINED_SYMBOLS=0 -s MAX_WEBGL_VERSION=2 -s USE_WEBGL2=1'
#-s FULL_ES3=1'
for f in `find . -name linklibs.rsp`; do 
    NEW=`cat $f | tr " " "\n" | sed "s/-lnul//" | sort -u | tr "\n" " "`
    echo $NEW > $f
done
